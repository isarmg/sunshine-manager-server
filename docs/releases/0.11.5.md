# Sunshine Manager Server 0.11.5

- “新建实例”直接以默认名称创建，登录后进入实例列表，不再显示名称输入弹窗。
- 实例统计与 Host Monitoring 统一为总数及按系统分类的“总数 / 在线”表格。
- 配对信息统一为账户名、不可变内部账户 ID 和可更换密码；新建及更换密码均为 32 位小写英文字母或数字，Client 仍只输入授权码。
- 实例接口一次返回全部记录，并按账户名忽略大小写稳定排序；升级至 Foundation 0.8.8。
